# -*- coding: utf-8 -*-
"""
Created on Sun Mar 10 11:54:01 2019

@author: user
"""

"""
                    try:
                        y = translateException(analysis,"fr")
                        #print("wooooord")
                        raise Exception(str(analysis))
                        #raise Exception(analysis.translate(to='fr'),"fr")
                        #print(y)
                    except Exception as e:
                        print(type(e))
                        x = e.args
                        #y = str(x)
                        print( str(x))
                        #print(type(ve), '::', ve)
                        #print("did it work ??")
                        newSentence = newSentence + " " + str(y) 
                        #print(newSentence)
                    try:
                        #y = translateException(analysis,"fr")
                        #print("wooooord")
                        raise Exception(str(analysis))
                        #raise Exception(analysis.translate(to='fr'),"fr")
                        #print(y)
                    except Exception as e:
                        print(type(e))
                        x = e.args
                        #y = str(x)
                        print(x)
                        #print(type(ve), '::', ve)
                        #print("did it work ??")
                        wordTranslated = analysis.translate(to='fr')
                        newSentence = newSentence + " " + str(wordTranslated) 
                        #print(newSentence)
                        
                        wordTranslated = analysis.translate(to='fr')
                        #newSentence = ' '.join(wordTranslated)
                        newSentence = newSentence + " " + str(wordTranslated)
        
            
                try:
                    raise ValueError('Translation API returned the input string unchanged.')
                    raise Exception(wordTranslated = analysis.translate(to='fr'),newSentence = newSentence + " " + str(wordTranslated))
                    #raise Exception(newSentence = newSentence + " " + str(wordTranslated))
                    #raise Exception('This is the exception you expect to handle')
                except Exception as error:
                    #wordTranslated=analysis.translate(to='fr')
                    print(wordTranslated)
                    print(newSentence)
                    #print('Caught this error:  + repr(error)')
                #analysis.translate(from_lang=curentLang,to='fr')
                #if analysis.translate(to='fr') == analysis:
                     #newSentence = newSentence + " " + str(word) 
                #elif analysis.translate(to='fr') == analysis: 
                    #curentLang = analysis.detect_language()
                    #wordTranslated = analysis.translate(to='fr')
                    #newSentence = ' '.join(wordTranslated)
                    #newSentence = newSentence + " " + str(wordTranslated)
                    #print("2")
                """